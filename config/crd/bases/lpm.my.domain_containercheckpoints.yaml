---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: containercheckpoints.lpm.my.domain
spec:
  group: lpm.my.domain
  names:
    kind: ContainerCheckpoint
    listKind: ContainerCheckpointList
    plural: containercheckpoints
    singular: containercheckpoint
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: ContainerCheckpoint is the Schema for the containercheckpoints
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ContainerCheckpointSpec defines the desired state of ContainerCheckpoint.
            properties:
              containerName:
                type: string
              podName:
                type: string
            required:
            - containerName
            - podName
            type: object
          status:
            description: ContainerCheckpointStatus defines the observed state of ContainerCheckpoint.
            properties:
              boundContentName:
                description: 'BoundContentName: cluster-scoped ContainerCheckpointContent
                  name.'
                type: string
              completionTime:
                format: date-time
                type: string
              message:
                type: string
              phase:
                type: string
              ready:
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
